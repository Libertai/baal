# Telegram bot token from @BotFather
TELEGRAM_BOT_TOKEN=

# LibertAI API key (bot operator's default key for free-tier users)
LIBERTAI_API_KEY=

# Hex-encoded Ethereum private key for Aleph Cloud instance creation
ALEPH_PRIVATE_KEY=

# SSH public key injected into VMs for deployment access
ALEPH_SSH_PUBKEY=

# Path to SSH private key for connecting to VMs
ALEPH_SSH_PRIVKEY_PATH=~/.ssh/id_rsa

# Fernet key for encrypting user API keys at rest
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
BOT_ENCRYPTION_KEY=
